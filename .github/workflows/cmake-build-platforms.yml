# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: Build and Test with CMake on Multiple Platforms

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: false

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        os: [ubuntu-latest, windows-latest]
        build_type: [Release]
        c_compiler: [gcc, cl]
        python-version: ['3.8', '3.11']
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
          - os: ubuntu-latest
            c_compiler: gcc
            cpp_compiler: g++
        exclude:
          - os: windows-latest
            c_compiler: gcc
          - os: windows-latest
            c_compiler: clang
          - os: ubuntu-latest
            c_compiler: cl

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install OpenCL
      if: matrix.os == 'ubuntu-latest'
      run: sudo apt-get install -y ocl-icd-opencl-dev pocl-opencl-icd

    - name: Install OpenCL
      if: matrix.os == 'windows-latest'
      run: choco install -y opencl-intel-cpu-runtime

    - uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}
        -DCMAKE_C_COMPILER=${{ matrix.c_compiler }}
        -DCMAKE_CXX_STANDARD=20
        -DBUILD_SAMPLE=ON
        -DBUILD_PYMS3D=ON
        -S ${{ github.workspace }}

    - name: Build
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

    - name: Run C++ Sample
      if: matrix.os == 'windows-latest'
      run: >
        ${{ steps.strings.outputs.build-output-dir }}/core/${{ matrix.build_type }}/mscomplex3d-sample.exe 
        ${{ github.workspace }}/testing/Marschner-Lobb_41x41x41.raw 41 41 41

    - name: Run C++ Sample
      if: matrix.os == 'ubuntu-latest'
      run: >
        ${{ steps.strings.outputs.build-output-dir }}/core/mscomplex3d-sample
        ${{ github.workspace }}/testing/Marschner-Lobb_41x41x41.raw 41 41 41

    - name: Install Python package
      if: matrix.os == 'windows-latest'
      run: cp ${{ steps.strings.outputs.build-output-dir }}/pyms3d/**/*.pyd ./

    - name: List pyms3d build output directory
      if: matrix.os == 'ubuntu-latest'
      run: ls -R ${{ steps.strings.outputs.build-output-dir }}/pyms3d

    - name: Install Python package
      if: matrix.os == 'ubuntu-latest'
      run: cp ${{ steps.strings.outputs.build-output-dir }}/pyms3d/*.so ./

    - name: Install pytest
      run: pip install pytest
      
    - name: Test Python package
      run: |
        pytest -v .
